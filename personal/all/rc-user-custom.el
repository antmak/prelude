;;; rc-user-custom.el --- Description
;;; Commentary:

;;; Code:
;(unless (string-equal "root" (getenv "USER"))
; ;; Only start server mode if I'm not root
;  (require 'server)
;  (server-start))

;;; rc-user-custom.el ends here
